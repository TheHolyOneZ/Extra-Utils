# Extra-Utils
**Extra Utils** is a Discord bot extension that provides a collection of utility commands for server management and information.
> 💡 **Built for the Zygnal Ecosystem — to download and use this extension, you must be part of the Zygnal Ecosystem.**  
> This extension (cog) is part of the **Zygnal Ecosystem** and is only available through its supported platforms.  
> You can use it with:  
> - The **[Discord Bot Framework](https://github.com/TheHolyOneZ/discord-bot-framework)** — ideal for developers who want full control and flexibility *(includes an integrated extension marketplace)*, or  
> - The **[ZygnalBot](https://zygnalbot.de)** — a prebuilt, plug-and-play Discord bot *(also includes an integrated extension marketplace)*.  
>
> Browse and install extensions at [zygnalbot.com/extension](https://zygnalbot.com/extension).  
> For help or community discussions, join us on Discord: [discord.gg/sgZnXca5ts](https://discord.gg/sgZnXca5ts)
# Extra Utils

**Extra Utils** is a Discord bot extension that provides a collection of utility commands for server management and information.

---

## 📜 Features and Commands

### ✉️ Message & Channel Utilities
- `!firstmsg` — Jumps to the first message ever sent in the channel.
- `!lastmsg` — Jumps to the most recent message in the channel.
- `!jumpto <messageID>` — Navigates to a specific message using its ID.
- `!searchmsg <text>` — Finds and links to the first message in the last 500 messages that contains the specified text.
- `!randommsg` — Selects and links to a random message from the last 500 messages in the channel.
- `!clearpins` — Unpins all pinned messages in the current channel.

---

### 👥 Member & Role Management
- `!nickall <name>` — Changes the nickname of all members in the server to a new name.
- `!resetnicks` — Resets the nicknames of all members in the server to their default names.
- `!roleremoveall <role>` — Removes a specified role from all members who have it.
- `!rolebots <role>` — Adds or removes a specified role from all bot accounts in the server.
- `!rolehumans <role>` — Adds or removes a specified role from all human members in the server.

---

### ℹ️ Info & Stats
- `!serverage` — Displays the age of the Discord server.
- `!membercount` — Shows the total number of members in the server.
- `!onlinecount` — Displays the number of members who are currently online.
- `!rolecount <role>` — Counts and shows how many members have a specific role.
- `!joined @user` — Shows the date when a mentioned user joined the server.
- `!created @user` — Displays the date when a mentioned user's Discord account was created.
- `!randomuser` — Picks a random online human member from the server.
- `!ubanner @user` — Shows the profile banner of a mentioned user, if they have one.
